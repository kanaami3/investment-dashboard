# Copy this file to .env in the python/ directory (or set as real env vars).
# DO NOT commit .env — it is gitignored.

OANDA_TOKEN=put-your-personal-access-token-here
OANDA_ACCOUNT_ID=101-001-1234567-001
OANDA_ENV=practice
# OANDA_ENV=live   # only after demo verification + OANDA_ALLOW_LIVE=1
# OANDA_ALLOW_LIVE=1
# OANDA_DRY_RUN=true

# --- stock_tools ---
# Create a Discord webhook in Server Settings → Integrations → Webhooks,
# copy the URL, and paste it here.  DO NOT share or commit the real URL.
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/REPLACE/REPLACE
